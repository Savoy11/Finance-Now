# Code audit — 2026-07-30

**Commit:** `14d6d76f5208c12a990d55dd439fb3301d861092` (branch `chore/improvement-agents`) · **Checks run:** `npm ci`, `npm run type-check`, `npm run lint`, `npm test`, `npm run data-sources -- --verify`, `git log` · **Checks skipped:** `npm run audit` / `npm run smoke` (IP-dependent — see below), `npm run build` (cost)
> **Note (2026-09-08):** commit `14d6d76` predates the **2026-08-05 re-root of
> `main`** and is **not reachable from `main`** — `git show 14d6d76` fails on a
> fresh clone. It survives only on the archive branches; fetch
> `archive/pre-reset-main` (or the relevant `archive/pre-reset/*` tip) first.
> See CLAUDE.md § "How Changes Land" and
> `docs/audits/git-repo-audit-2026-08-23.md`.

> **Scope note.** This was commissioned as an **installation smoke test**, not a fortnightly audit.
> It confirms the auditor can read the repo, run the checks, and produce its report file. The
> finding count was capped at 2 by the request; this is **not** a full sweep and must not be read
> as a baseline. Prior leads live in `docs/PRELIMINARY-FINDINGS-2026-07-30.md`, which is explicitly
> not a baseline either.

## Summary

The repo is in good mechanical health: `tsc --noEmit` is clean, ESLint reports 0 errors, all 412
tests across 28 files pass, and the data-sources registry shows no drift against route code. Both
findings below are the same class — **a comment whose stated reason has expired**, which is the
category generic tooling misses. The one to act on first is the permanently-green "Live" indicator,
because it is on every screen and it can never report a degraded feed.

## Findings

### 1. The "Live" connection indicator is hardcoded and can never report a failure · `P1`

**Evidence:** `src/lib/websocket/hooks.ts:15-21` is the only writer of connection status in the codebase:

```ts
export function useWebSocket() {
  const { setConnectionStatus } = useStreamStore()

  useEffect(() => {
    setConnectionStatus('connected')
  }, [setConnectionStatus])
}
```

`grep -rn "setConnectionStatus" src/` returns exactly four hits — the store's type (`store/useStreamStore.ts:16`),
its setter (`:30`), and the two lines above. There is no other caller, and no error or failure path.

The value is rendered in two always-visible chrome surfaces:
- `src/components/layout/StatusBar.tsx:51` — `Data: {connectionStatus === 'connected' ? 'LIVE (polling)' : …}`, coloured `text-emerald-400` (`:25`)
- `src/components/layout/Sidebar.tsx:461-464` — a green status dot plus the label `Live` (`STATUS_LABELS`, `Sidebar.tsx:161-166`)

`useWebSocket()` is invoked unconditionally on every dashboard render (`src/app/(dashboard)/layout.tsx:17`).

**What is wrong:** The hook's own comment explains it is a "status shim" left behind after the M8
sweep removed the real socket client. The shim kept the *green* branch and dropped every other one.
`'connecting'`, `'disconnected'` and `'error'` are unreachable — dead branches in both components.

**Why it matters:** The app tells the user "Data: LIVE (polling)" in green while a `/live-data/*`
route is 503ing, rate-limited, or serving FALLBACK. That is a fabricated live signal on the exact
chrome a user checks *before* trusting a price — the same class of defect that de-routed
`/global-adoption` under T5, and it contradicts the README's stated data-honesty principle.
(Distinct from the `StatusBar.tsx:41` hardcoded market-`OPEN` literal already recorded in
`PRELIMINARY-FINDINGS-2026-07-30.md`; that is a different line and a different signal, though the
same class. Both sit in the same status bar, so a user sees two independent fabrications side by side.)

**Suggested fix:** Either drive the status from something real that already exists — React Query's
global fetching/error state is the obvious seam — or delete the indicator and its three unreachable
branches. A shim that only ever reports success is worse than no indicator.

*Importance:* High — always on screen, and it is the signal that gates trust in every number below it. · *Efficiency:* High — one hook, ~6 lines, plus removing dead branches. · *Practicality:* High — no data dependency; the React Query state is already in the tree.

### 2. The documented procedure for re-enabling the auth wall does not re-enable it · `P2`

**Evidence:** `src/app/(dashboard)/layout.tsx:19-23`:

```ts
// LOGIN TEMPORARILY DISABLED (by request, until the risk framework is done).
// The auth wall is fully off so it cannot affect the build or block access.
// To RE-ENABLE later: restore `import { LIVE_DATA } from '@/lib/constants'`
// and `const REQUIRE_AUTH = !LIVE_DATA`, and revert the /login redirect.
const REQUIRE_AUTH = false
```

`src/lib/constants.ts:14`:

```ts
export const LIVE_DATA = true
```

So the documented restoration, `const REQUIRE_AUTH = !LIVE_DATA`, evaluates to `false`. Following
the instruction exactly leaves the wall off. `LIVE_DATA` is not a toggle — `CLAUDE.md` states it is
hardcoded `true` with no env override, and `lib/websocket/hooks.ts:8` says the same in its own comment.

Two further contradictions in the same pair of files:
- `src/app/(auth)/login/page.tsx:16-19` gives a **different** recipe for the same task — "set this to false and set `REQUIRE_AUTH = true`" — which is the correct one. Two recipes, one broken.
- Both comments gate on "until the risk framework is done." That condition is met: `docs/architecture/risk-scale-spec.md` exists (47,661 bytes), `docs/TASK-QUEUE.md:36-42` records R1 as complete, and `README.md:57` states the Safety Score has been "🟢 **Live** since 2026-07-18." Nobody revisited the flag.

**What is wrong:** A safety control is off, its stated expiry condition has since been satisfied, and
the primary note on how to switch it back on is wrong.

**Why it matters:** Whoever re-enables auth — likely under time pressure, before a first
deployment — follows `layout.tsx` because that is where `REQUIRE_AUTH` lives, sees no change in
behaviour, and may conclude the wall is on when it is not. The blast radius is bounded today only
by `isLocalUserModeAllowed()` (`src/lib/auth/session.ts:44-48`), which denies local-user mode in
production by default; that default is the only thing standing between a public deploy and every
anonymous visitor sharing one account, as the comment at `session.ts:24-27` itself warns.

**Suggested fix:** Delete the `!LIVE_DATA` recipe, keep the single correct one in
`login/page.tsx`, and either act on the now-satisfied condition or replace it with a condition that
has not already passed.

*Importance:* Medium — no user-visible wrong number, but it is a security control with misleading operating instructions. · *Efficiency:* High — a comment correction plus one decision. · *Practicality:* High — the correct recipe is already written in the sibling file.

## Checks that could not be run

- **`npm run audit` / `npm run smoke`** — **not attempted, deliberately.** `CLAUDE.md` states data-availability results are IP-dependent and audits must run on the owner's machine (Binance geo-blocks this host with 451; Reddit and LunarCrush block datacenter IPs). A run from this container would produce a systematically wrong REAL-vs-FALLBACK baseline. Must be run by the owner.
- **`npm run build`** — skipped for cost under the smoke-test brief. It is expected to work now that dependencies are installed; before installation it failed with `sh: 1: next: not found`.
- **Test coverage** — not recorded. `npm test` is `vitest run` with no `--coverage` flag, so the run reports pass/fail only. There is no `test:coverage` script in `package.json`.

### Environment note, relevant to future runs

`frontend/node_modules` was absent at the start of this run. Every check failed against it:

```
npm run lint        → ERR_MODULE_NOT_FOUND (eslint)
npm test            → sh: 1: vitest: not found
npm run data-sources→ sh: 1: tsx: not found
npm run build       → sh: 1: next: not found
npm run type-check   → ran on a global tsc and emitted ~8 phantom errors
                       (TS7006 in useWatchlistStore.ts, TS2307 vitest/config,
                        TS2591 node:path) that vanish once deps are present
```

`npm ci --no-audit --no-fund` succeeded from this container (709 packages, exit 0), so the npm
registry **is** reachable through the proxy even though the live-data providers are not. **Future
runs should install first** — the phantom typecheck errors above are exactly the kind of false
positive that wastes an audit. Note also the script is `type-check`, not `typecheck`.

## Examined and clean

- `npm run type-check` — exit 0, no errors, after install.
- `npm run lint` — exit 0. **67 warnings, 0 errors**; sampled warnings are `react-hooks/set-state-in-effect` (e.g. `src/lib/watchlist/useWatchlistBias.ts:24`), a performance advisory, not a defect.
- `npm test` — **412 tests across 28 files, all passing**, 4.10s.
- `npm run data-sources -- --verify` — "Wrote DATA-SOURCES.md (43 surfaces)" and "✓ no drift: every route host is represented in the registry." The generated file matched; no hand-edit detected.
- **Affiliate/referral scan** — `grep -rniE "(\?|&)(ref|aff|affiliate|referral|partner|utm_source=financenow|pid|clickid)="` across all `.ts`/`.tsx` in `src/` returned **zero matches**. Clean.
- **`TODO`/`FIXME`/`HACK` scan** — `grep -rnE "(TODO|FIXME|HACK|XXX):"` across `src/` returned **zero matches**. Clean.
- **`/risk-scores` provenance** — the leaderboard does render coverage (`risk-scores/page.tsx:157`) alongside each score and discloses the snapshot date (`:271`, `curated issuer disclosures as of {metaAsOf}`), satisfying the "no score without its coverage figure" policy. The reserve pillar's attestation-age comment (`app/live-data/risk-scores/route.ts:223-236`) was tested against current data and its *reasoning* still holds. Two response fields, `metaStale` and `metaAgeDays`, have zero consumers in `src/` — noted but not raised, as `metaAsOf` covers the user-facing disclosure and `metaStale` is currently `false` anyway.
- **`README.md` "750 coins"** (`README.md:51`) versus 22 entries in `CoinId` (`lib/data/transferFees.ts:191-194`) was investigated and **discarded as a finding**: `transfer-fees/page.tsx:447,481` backs the selector with a live `coin-list` feed, so the claim is defensible and the discrepancy is not clean enough to cite.

## Not covered by this run

This was a 2-finding smoke test. Untouched: the ~15 unverified leads in
`docs/PRELIMINARY-FINDINGS-2026-07-30.md`, all live-data provenance, the risk-scale policy sweep
across `stakingProviders.ts` (tracked as R2 in `docs/TASK-QUEUE.md:489`), the AI-content labelling
policy, and every module page. A first real baseline still needs a full pass.

One feature-shaped observation, passed to `opportunity-scout` rather than pursued here: the removed
websocket seam in `lib/websocket/hooks.ts:12-14` is documented as the reopening point for a real
streaming layer.
