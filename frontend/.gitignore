
# ESLint SARIF report (generated in CI for code scanning upload)
eslint-results.sarif

# Fee reconcile outputs (owner-machine artifacts, not source)
transfer-fee-reconcile.md
transfer-fee-reconcile.json
# Fund-fee reconcile outputs (npm run fund-fees) — owner-machine artifacts
fund-fee-reconcile.json
fund-fee-worksheet.csv

# Audit outputs (generated by npm run audit:json / audit)
audit.json
audit.txt

# Vitest coverage output (npm run test:coverage)
coverage/

# Aggregate affiliate click counts (no user data — see lib/server/affiliateClicks.ts)
.affiliate-clicks.json
